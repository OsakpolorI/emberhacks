# Issue 1 — bootstrap and verification

Issue: https://github.com/OsakpolorI/emberhacks/issues/1
Owner: OsakpolorI / Codex integrator. Sequential bootstrap; no parallel workers.
Owned files: src/**, public/audio-worklet.js, scripts/**, tests/**, root configs, dependencies, initial docs.
Branch: main bootstrap; future tasks must use issue branches/worktrees.

## Current checkpoint

- App, live adapter, evidence endpoint, dashboard, fixture preview, collaboration docs and CI implemented.
- 28 tests, typecheck, production build and client-bundle secret scan pass.
- Gemini 3.8 Flash inference, ephemeral token minting, Gemini 3.8 Live audio response and two synthetic-audio follow-ups passed. The Live smoke was repeated after upgrading the SDK.
- Real analyst calls exposed intermittent provider overload. Added a one-attempt Gemini 3.6 Flash fallback and a live-analysis quota cooldown. The final endpoint returned a grounded HTTP 200 result on a contradiction fixture.
- Browser empty/sample/reset/setup paths checked. Microphone permission requires the local browser prompt; real human rehearsals are not yet certified.
- Existing default Gemini API key configured in ignored .env.local. No key in repository.

## Next action

Push and tag the verified implementation. Human rehearsal and video remain a separate task. See `docs/VERIFICATION.md`.

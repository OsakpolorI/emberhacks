'use client';
import { CartesianGrid, Line, LineChart, ReferenceLine, ResponsiveContainer, Tooltip, XAxis, YAxis } from 'recharts';
import { type AssessmentPoint, type Signal } from '@/lib/contracts';
export function SuspicionChart({points,elapsed,onSelect}:{points:AssessmentPoint[];elapsed:number;onSelect:(point:AssessmentPoint)=>void}){
 return <div className="chart-wrap" aria-label="AI suspicion over time, an uncalibrated game score"><ResponsiveContainer width="100%" height="100%"><LineChart data={points} margin={{top:18,right:15,left:-24,bottom:2}} onClick={event=>{const index=event?.activeTooltipIndex;if(index!=null&&points[Number(index)])onSelect(points[Number(index)]);}}><CartesianGrid stroke="#eaeaf0" strokeDasharray="3 5" vertical={false}/><XAxis type="number" dataKey="timestamp" domain={[0,Math.max(60,Math.ceil(elapsed/30)*30)]} tickFormatter={n=>`${Math.floor(n/60)}:${String(Math.floor(n%60)).padStart(2,'0')}`} axisLine={false} tickLine={false} tick={{fill:'#9294a5',fontSize:11}} tickMargin={14}/><YAxis domain={[0,100]} ticks={[0,25,50,75,100]} axisLine={false} tickLine={false} tick={{fill:'#9294a5',fontSize:11}}/><ReferenceLine y={50} stroke="#dad9e8" strokeDasharray="4 5"/><Tooltip content={({active,payload})=>{if(!active||!payload?.length)return null;const p=payload[0].payload as AssessmentPoint;return <div className="chart-tooltip"><b>{p.suspicionScore??'—'} / 100 · {p.evidenceStrength} evidence</b><p>{p.explanation}</p>{p.evidence.flatMap(e=>e.quotes).slice(0,2).map((q,i)=><small key={i}>“{q.text}”</small>)}</div>;}}/><Line type="stepAfter" dataKey="suspicionScore" stroke="#7772ee" strokeWidth={3} connectNulls={false} isAnimationActive={false} dot={{r:4,fill:'#fff',strokeWidth:2}} activeDot={{r:7}}/></LineChart></ResponsiveContainer>{!points.length&&<div className="chart-empty"><span className="empty-orbit">∿</span><strong>Every story leaves a trail.</strong><span>Your first assessment will appear here as you speak.</span></div>}</div>;
}
export function ActivityChart({signals}:{signals:Signal[]}){const visible=signals.slice(-110);return <div className="activity-bars" aria-label="Measured microphone activity">{Array.from({length:110},(_,i)=>{const sample=visible[i-(110-visible.length)];return <i key={i} className={sample?.aiSpeaking?'ai-bar':sample?.speaking?'speech-bar':''} style={{height:`${sample?Math.min(38,Math.max(3,sample.level*350)):3}px`}}/>;})}</div>;}
